apiVersion: v1
kind: Service
metadata:
  name: mlflow
  namespace: application
spec:
  type: ClusterIP
  selector:
    app: mlflow
    app.kubernetes.io/instance: mlflow
    app.kubernetes.io/name: mlflow
  ports:
    - name: http
      port: 5000
      targetPort: 5000
